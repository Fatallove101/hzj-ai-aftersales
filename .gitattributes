# 统一换行符，避免 Windows 上反复出现 CRLF 警告
* text=auto

# PowerShell / 批处理在 Windows 上用 CRLF
*.ps1   text eol=crlf
*.bat   text eol=crlf

# 数据文件固定 LF，避免 diff 噪音
*.csv   text eol=lf
*.md    text eol=lf
*.json  text eol=lf
*.sql   text eol=lf

# 二进制，不做任何转换
*.png   binary
*.jpg   binary
*.docx  binary
